<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Configure debug-overrides to trust user-installed CA certificates (like JMeter's root certificate) in debug/profile builds -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="user" />
            <certificates src="system" />
        </trust-anchors>
    </debug-overrides>
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
